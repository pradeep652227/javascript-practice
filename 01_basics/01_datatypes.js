"use strict";//by default it is enabled by the browsers (nowadays)
//to use latest standards of js

console.table([
    typeof 12,
    typeof BigInt,
    typeof null,
    typeof undefined,
    typeof symbol
]);

